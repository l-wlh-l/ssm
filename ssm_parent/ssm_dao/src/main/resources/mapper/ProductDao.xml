<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wlh.ssm.dao.ProductDao">
    <select id="findAll" resultType="Product" >
        select * from product
    </select>

    <select id="findById" resultType="Product" parameterType="Long">
        select * from product where id = #{productNum}
    </select>
    <delete id="deleteProduct" parameterType="Long">
        delete from product where id=#{productNum}
    </delete>

    <insert id="saveProduct" parameterType="product">
        insert into product
            (  id,  productNum,     productName,  cityName,   departureTime,   productPrice, productDesc,  productStatus)
        values
            (#{id},#{productNum},#{productName},#{cityName},#{departureTime},#{productPrice},#{productDesc},#{productStatus})
    </insert>

    <update id="updateProduct" parameterType="product">
        update product set
          productNum = #{productNum},productName=#
          {productName},cityName=#{cityName},departureTime=#{departureTime},productPrice=#
          {productPrice},productDesc=#{productDesc},productStatus=#{productStatus}

           where id = #{productNum}
    </update>
</mapper>
